<IfModule mod_rewrite.c>

    #Activar rewrite
    RewriteEngine on
    Options All -Indexes
    Options -MultiViews
    #ErrorDocument 404 http://localhost/proyecto-cursos/

    # RewriteRule ^(.*)/(.*) index.php?controller=$1&action=$2 

</IfModule>